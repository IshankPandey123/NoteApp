<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen text-white font-[Inter]">

  <!-- Header -->
  <header class="text-center py-10">
    <h1 class="text-5xl font-bold tracking-tight bg-gradient-to-r from-blue-400 to-green-400 bg-clip-text text-transparent">
      Task Manager
    </h1>
    <p class="text-zinc-400 mt-2">Organize your tasks efficiently ✨</p>
  </header>

  <!-- Form Section -->
  <section class="max-w-2xl mx-auto bg-zinc-800/60 backdrop-blur-sm p-8 rounded-2xl shadow-lg border border-zinc-700">
    <form action = "/create" method = "post" class="space-y-4">
      <input type="text"
        class="w-full px-5 py-3 bg-zinc-900 border border-zinc-700 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none"
        placeholder="Task Title" name = "title">

      <textarea rows="4"
        class="w-full px-5 py-3 bg-zinc-900 border border-zinc-700 rounded-lg resize-none focus:ring-2 focus:ring-blue-500 outline-none"
        placeholder="Write your task details here..." name = "details"></textarea>

      <input
        class="w-full py-3 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-500 hover:to-indigo-500 rounded-lg font-semibold cursor-pointer transition duration-300"
        type="submit" value="Create Task">
    </form>
  </section>

  <!-- Task List -->
  <section class="p-10 flex flex-wrap justify-center gap-6">
    <!-- Task Card Example -->
    <% if(files.length > 0){ %>
        <% files.forEach(function(val){ %>
            <div class="w-72 p-6 rounded-xl bg-zinc-800 border border-zinc-700 shadow-md hover:shadow-lg hover:-translate-y-1 transition-all duration-300">
                <h1 class="text-xl font-semibold text-white tracking-tight">
                  <%=val%>
                </h1>
               <!-- Action Bar -->
<div class="flex w-full justify-between items-center mt-5 border-t border-zinc-700/50 pt-3">
    <!-- Read More Button -->
    <a href="/file/<%= val %>"
      class="flex items-center gap-2 text-blue-400 hover:text-green-400 font-medium transition-all duration-300 group">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
        stroke-width="2" stroke="currentColor"
        class="w-5 h-5 opacity-80 group-hover:translate-x-1 transition-transform duration-300">
        <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
      </svg>
      <span class="group-hover:underline">Read More</span>
    </a>
  
    <!-- Edit Button -->
    <a href="/edit/<%= val %>"
      class="flex items-center gap-2 text-zinc-500 hover:text-yellow-400 font-medium transition-all duration-300 group">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
        stroke-width="2" stroke="currentColor"
        class="w-5 h-5 opacity-80 group-hover:rotate-12 transition-transform duration-300">
        <path stroke-linecap="round" stroke-linejoin="round"
          d="M15.232 5.232l3.536 3.536M16.732 3.732a2.5 2.5 0 113.536 3.536L7.5 20.036H4v-3.572L16.732 3.732z" />
      </svg>
      <span class="group-hover:underline">Edit</span>
    </a>
  </div>
  
                
              </div>
            <%})%>
    <%} else{ %>
        <!-- Empty State -->
    <h3 class="text-zinc-600 italic">No Tasks Yet! Create your first one </h3>
    <%}%>

    
  </section>

</body>

</html>
