<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Task | Task Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen flex flex-col items-center text-white font-[Inter]">

  <!-- Header -->
  <header class="text-center py-10">
    <h1 class="text-5xl font-bold tracking-tight bg-gradient-to-r from-blue-400 via-green-400 to-yellow-400 bg-clip-text text-transparent drop-shadow-lg">
      Edit Task
    </h1>
    <p class="text-zinc-400 mt-2 text-sm md:text-base">Modify your task name with ease ⚙️</p>
  </header>

  <!-- Back Button -->
  <div class="mb-6">
    <a href="/"
      class="flex items-center gap-2 bg-gradient-to-r from-blue-600 to-green-500 px-6 py-2 rounded-full text-white font-semibold shadow-lg shadow-green-500/30 hover:shadow-green-400/40 hover:scale-105 transition-all duration-300">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
        stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
      </svg>
      Back to Home
    </a>
  </div>

  <!-- Edit Form Card -->
  <div
    class="backdrop-blur-md bg-white/10 border border-white/10 p-8 rounded-2xl shadow-xl w-[90%] max-w-md text-center">
    <form action="/edit" method="post" class="space-y-6">
      <!-- Previous Name -->
      <div>
        <label class="block text-sm text-zinc-400 mb-2">Previous Name</label>
        <input type="text"
          class="w-full px-5 py-3 bg-zinc-900 border border-zinc-700 rounded-lg text-zinc-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none disabled:opacity-60"
          name="previous" value="<%= filename %>" readonly>
      </div>

      <!-- Next Name -->
      <div>
        <label class="block text-sm text-zinc-400 mb-2">New Name</label>
        <input type="text"
          class="w-full px-5 py-3 bg-zinc-900 border border-zinc-700 rounded-lg text-zinc-300 focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none"
          placeholder="Enter new name..." name="next" required>
      </div>

      <!-- Submit Button -->
      <input
        class="w-full py-3 bg-gradient-to-r from-green-500 to-blue-600 hover:from-blue-600 hover:to-indigo-600 rounded-lg font-semibold cursor-pointer transition duration-300 shadow-md hover:shadow-blue-500/40"
        type="submit" value="Update Name">
    </form>
  </div>

  <!-- Footer -->
  <footer class="text-zinc-500 text-sm mt-10 mb-6">
    © 2025 Task Manager | Designed with ❤️ by Ishank
  </footer>

</body>
</html>
